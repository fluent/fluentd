version: '{build}'

# init:
#   - ps: iex ((new-object net.webclient).DownloadString('https://raw.githubusercontent.com/appveyor/ci/master/scripts/enable-rdp.ps1'))

install:
  - SET PATH=C:\Ruby%ruby_version%\bin;%PATH%
  - "%devkit%\\devkitvars.bat"
  - ruby --version
  - gem --version
  - bundle install
build: off
test_script:
  - bundle exec ruby ./test_a.rb

branches:
  only:
    - master
    - v0.10
    - v0.12
    - v0.14

environment:
  matrix:
    - ruby_version: "23-x64"
      devkit: C:\Ruby23-x64\DevKit
matrix:
  allow_failures:
    - ruby_version: "21"
