<source>
  @type forward
</source>

<match test>
  @type file
  path /var/log/fluentd/out_file_test
  format json
  buffer_type memory
  # compress gzip
  # symlink_path /path/to/symlink
  append false
</match>
