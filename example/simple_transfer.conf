<worker>
  <source>
    type dummy_data
    tag  test.hoge
    data {"message":"This is dummyyyyyyyyy"}
    # interval 60s

    <match **>
      type forward
      flush_interval 1s
      <server>
        host localhost
        port 24225
      </server>
    </match>
  </source>
</worker>
