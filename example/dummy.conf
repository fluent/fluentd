<worker>
  <source>
    type dummy_data
    data {"message":"This is dummyyyyyyyyy"}
  </source>

  # TODO: Don't work yet!
  <match ** copy>
    type stdout
  </match>

  <match **>
    type stdout
  </match>
</worker>
