<source>
  @type dummy
  tag test
</source>

<match test>
  @type forward
  transport tls
  tls_insecure_mode true
  <server>
    # first server
    host 127.0.0.1
    port 24224
  </server>
  <buffer>
    flush_interval 0
  </buffer>
</match>
