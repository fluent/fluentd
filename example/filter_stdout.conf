<source>
  @type dummy
  tag dummy
</source>

<filter **>
  @type stdout
</filter>

<filter **>
  @type stdout
  output_type hash
</filter>

<filter **>
  @type stdout
  format ltsv
</filter>

<match **>
  @type null
</match>
